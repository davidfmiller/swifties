// swift-tools-version: 6.2

import PackageDescription

let package = Package(
  name: "json-min",
  platforms: [.macOS(.v10_15)],
  dependencies: [
  ],
  targets: [
    .executableTarget(
      name: "json-min",
      dependencies: [
      ]
    ),
  ]
)
