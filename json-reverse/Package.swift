// swift-tools-version: 6.2

import PackageDescription

let package = Package(
  name: "json-reverse",
  platforms: [.macOS(.v10_15)],
  dependencies: [
  ],
  targets: [
    .executableTarget(
      name: "json-reverse",
      dependencies: [
      ]
    ),
  ]
)
