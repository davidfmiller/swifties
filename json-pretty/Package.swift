// swift-tools-version: 6.2

import PackageDescription

let package = Package(
  name: "json-pretty",
  platforms: [.macOS(.v10_15)],
  dependencies: [
  ],
  targets: [
    .executableTarget(
      name: "json-pretty",
      dependencies: [
      ]
    ),
  ]
)
